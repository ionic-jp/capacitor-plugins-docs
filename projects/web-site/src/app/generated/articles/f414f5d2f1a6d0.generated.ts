// Generated by scripts/generate-articles.ts. Do not edit.
export default {"html":"<p data-line=\"0\" class=\"code-line\"><a href=\"https://twitter.com/PentaPROgram/status/1550787703230574592\" target=\"_blank\" rel=\"noopener noreferrer\">https://twitter.com/PentaPROgram/status/1550787703230574592</a></p>\n<p data-line=\"2\" class=\"code-line\">I came across that tweet and realized I had never written this up, so here is a summary of the workaround. This is a mobile Safari (WKWebView) bug, and it still has not been fixed. By the way, unless you are using Cordova/Capacitor or building your own WebView app, this bug does not affect you.</p>\n<p data-line=\"4\" class=\"code-line\"><a href=\"https://bugs.webkit.org/show_bug.cgi?id=226023\" target=\"_blank\" rel=\"noopener noreferrer\">https://bugs.webkit.org/show_bug.cgi?id=226023</a></p>\n<p data-line=\"6\" class=\"code-line\">The explanation below uses <code>ion-input</code>, but from the issues it looks like plain <code>input</code> elements reproduce it too.</p>\n<h2 id=\"why-it-happens\" data-line=\"8\" class=\"code-line\"><a class=\"header-anchor-link\" href=\"#why-it-happens\" aria-hidden=\"true\"></a> Why It Happens</h2>\n<p data-line=\"10\" class=\"code-line\">When autocomplete fills the field, WebKit does not fire an <code>input</code> event.</p>\n<h2 id=\"how-to-fix-it\" data-line=\"12\" class=\"code-line\"><a class=\"header-anchor-link\" href=\"#how-to-fix-it\" aria-hidden=\"true\"></a> How to Fix It</h2>\n<p data-line=\"14\" class=\"code-line\">The <code>change</code> event does fire, so listen for <code>change</code> and pass the value through.</p>\n<div class=\"code-block-container\"><pre class=\"shiki github-dark\" style=\"background-color:#151e2c;color:#e1e4e8\"><code class=\"code-line\" data-line=\"16\"><span class=\"line\"><span>&lt;ion-input #emailInput [(ngModel)]=\"email\" type=\"email\" name=\"email\"&gt;&lt;/ion-input&gt;</span></span>\n<span class=\"line\"><span></span></span>\n<span class=\"line\"><span></span></span></code></pre></div><p data-line=\"21\" class=\"code-line\">Suppose you have an element like that. Here is how to make it work with autocomplete. I will explain based on a basic Angular component.</p>\n<div class=\"code-block-container\"><pre class=\"shiki github-dark\" style=\"background-color:#151e2c;color:#e1e4e8\"><code class=\"code-line\" data-line=\"23\"><span class=\"line\"><span>  @Component({</span></span>\n<span class=\"line\"><span>    selector: 'app-home',</span></span>\n<span class=\"line\"><span>    templateUrl: 'home.page.html',</span></span>\n<span class=\"line\"><span>    styleUrls: ['home.page.scss'],</span></span>\n<span class=\"line\"><span>  })</span></span>\n<span class=\"line\"><span>  export class HomePage {</span></span>\n<span class=\"line\"><span>+   public email = '';</span></span>\n<span class=\"line\"><span>+   @ViewChild('emailInput', { static: true }) emailInput: IonInput;</span></span>\n<span class=\"line\"><span>  }</span></span>\n<span class=\"line\"><span></span></span>\n<span class=\"line\"><span></span></span></code></pre></div><p data-line=\"36\" class=\"code-line\">First, bind the element value to an <code>email</code> property. Because the element is named <code>#emailInput</code>, use <code>ViewChild</code> to reference it through an <code>emailInput</code> property. Next, watch the element's <code>change</code> event.</p>\n<div class=\"code-block-container\"><pre class=\"shiki github-dark\" style=\"background-color:#151e2c;color:#e1e4e8\"><code class=\"code-line\" data-line=\"38\"><span class=\"line\"><span>+ async ngOnInit() {</span></span>\n<span class=\"line\"><span>+   const nativeEmailInput = await this.emailInput.getInputElement();</span></span>\n<span class=\"line\"><span>+   nativeEmailInput.addEventListener('change', (ev: Event) =&gt; {</span></span>\n<span class=\"line\"><span>+     requestAnimationFrame(() =&gt; {</span></span>\n<span class=\"line\"><span>+       this.email = (ev.target as HTMLInputElement).value;</span></span>\n<span class=\"line\"><span>+     });</span></span>\n<span class=\"line\"><span>+   });</span></span>\n<span class=\"line\"><span>+ }</span></span>\n<span class=\"line\"><span></span></span>\n<span class=\"line\"><span></span></span></code></pre></div><p data-line=\"50\" class=\"code-line\"><code>ion-input</code> wraps an input element that you can get with <code>getInputElement</code>. When the <code>ngOnInit</code> lifecycle hook runs, call <code>this.emailInput.getInputElement()</code> to get that input. Then use <code>addEventListener</code> to watch <code>change</code> and copy the detected value into the <code>email</code> property.</p>\n<p data-line=\"52\" class=\"code-line\">The catch with this approach is that <code>change</code> keeps firing even when the user types normally, so the value keeps updating. That is why I use <code>requestAnimationFrame</code> so the value from <code>change</code> is applied after normal binding. It is not very elegant. If you are willing to exclude the case where the user starts typing and then switches to autocomplete, you could do something like this instead:</p>\n<div class=\"code-block-container\"><pre class=\"shiki github-dark\" style=\"background-color:#151e2c;color:#e1e4e8\"><code class=\"code-line\" data-line=\"54\"><span class=\"line\"><span>async ngOnInit() {</span></span>\n<span class=\"line\"><span>  const nativeEmailInput = await this.emailInput.getInputElement();</span></span>\n<span class=\"line\"><span></span></span>\n<span class=\"line\"><span>  nativeEmailInput.addEventListener('change', (ev: Event) =&gt; {</span></span>\n<span class=\"line\"><span>    if (this.email.length === 0) {</span></span>\n<span class=\"line\"><span>      this.email = (ev.target as HTMLInputElement).value;</span></span>\n<span class=\"line\"><span>    };</span></span>\n<span class=\"line\"><span>  });</span></span>\n<span class=\"line\"><span>}</span></span>\n<span class=\"line\"><span></span></span>\n<span class=\"line\"><span></span></span></code></pre></div><p data-line=\"67\" class=\"code-line\">That way the value is applied only when <code>email</code> is empty. If you only need the first <code>change</code> event, you can remove the listener immediately after it fires.</p>\n<p data-line=\"69\" class=\"code-line\">Related issue: <a href=\"https://github.com/ionic-team/ionic-framework/issues/23335\" target=\"_blank\" rel=\"noopener noreferrer\">https://github.com/ionic-team/ionic-framework/issues/23335</a></p>\n<p data-line=\"71\" class=\"code-line\">Browser bug workarounds are never pretty, but I hope this helps.<br>\nSee you next time.</p>\n","headings":[{"id":"why-it-happens","text":"Why It Happens","level":2},{"id":"how-to-fix-it","text":"How to Fix It","level":2}]} as const;
