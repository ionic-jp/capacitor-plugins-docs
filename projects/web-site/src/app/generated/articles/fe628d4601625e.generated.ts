// Generated by scripts/generate-articles.ts. Do not edit.
export default {"html":"<p data-line=\"0\" class=\"code-line\">I often hear teams put off CI because setup feels tedious, or they think things are fine as they are. When I join a project for code review, I introduce CI there too without hesitation. Here is a short summary of what I tell them.</p>\n<hr data-line=\"2\" class=\"code-line\">\n<p data-line=\"4\" class=\"code-line\">The purpose of CI is to reduce developer burden. More precisely, CI lets developers work more easily.</p>\n<p data-line=\"6\" class=\"code-line\">If you treat the code on GitHub shared by the whole team as the source of truth and the code each developer clones locally as a derivative, then before opening a pull request from derivative to source you normally need to confirm, assuming functional requirements are already met:</p>\n<ul data-line=\"8\" class=\"code-line\">\n<li data-line=\"8\" class=\"code-line\">Does it build in other environments too? (Is it tied only to your machine? / <code>build</code>)</li>\n<li data-line=\"9\" class=\"code-line\">Is the code formatted the way the team agreed? (Line length, commas, single quotes, and so on / <code>prettier</code>)</li>\n<li data-line=\"10\" class=\"code-line\">Does the code follow the TypeScript style the team agreed on? (No <code>any</code>, no unused declared variables, and so on / <code>es-lint</code>)</li>\n</ul>\n<p data-line=\"12\" class=\"code-line\">For people reviewing pull requests, it is also desirable to have:</p>\n<ul data-line=\"14\" class=\"code-line\">\n<li data-line=\"14\" class=\"code-line\">A preview URL for each pull request</li>\n<li data-line=\"15\" class=\"code-line\">Lighthouse scores for each pull request</li>\n</ul>\n<p data-line=\"17\" class=\"code-line\">Without CI, the person opening a pull request must run all of these checks locally without missing any, and attach the preview URL and Lighthouse results. Reviewers deciding whether to merge into <code>main</code> would then have to verify locally each time that those checks happened. That is unbearable. Depending on the work, it can happen dozens of times a day!</p>\n<p data-line=\"19\" class=\"code-line\">CI exists to eliminate that cost. Every push or pull request to GitHub runs the configured Actions automatically and reports the results, so developers are freed from repeating those checks by hand. Automate what can be automated, and focus on work only humans need to do!</p>\n<h2 id=\"commonly-used-actions\" data-line=\"21\" class=\"code-line\"><a class=\"header-anchor-link\" href=\"#commonly-used-actions\" aria-hidden=\"true\"></a> Commonly Used Actions</h2>\n<ul data-line=\"23\" class=\"code-line\">\n<li data-line=\"23\" class=\"code-line\">build: Does <code>npm run build</code> succeed? This catches simple mistakes and environment-specific dependencies (for example, packages installed globally with <code>npm i -g</code>)</li>\n<li data-line=\"24\" class=\"code-line\">lint: After configuring team rules with <code>es-lint</code> and <code>prettier</code>, check for violations with those tools. You can also run this with <code>lint-staged</code>, but CI catches cases where that did not run or was bypassed</li>\n<li data-line=\"25\" class=\"code-line\">test: Run unit tests and e2e tests</li>\n<li data-line=\"26\" class=\"code-line\">doc: Generate documentation from code. TypeDoc is common</li>\n<li data-line=\"27\" class=\"code-line\">hosting: Deploy each pull request or the latest <code>main</code> branch to Firebase Hosting so reviewers can check from a URL</li>\n<li data-line=\"28\" class=\"code-line\">lighthouse: Measure audits with lighthouse-ci</li>\n</ul>\n","headings":[{"id":"commonly-used-actions","text":"Commonly Used Actions","level":2}]} as const;
